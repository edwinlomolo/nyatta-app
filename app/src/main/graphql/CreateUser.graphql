mutation CreateUser(
    $first_name: String!
    $last_name: String!
    $email: String!
    $phone: String!
) {
    createUser(input: {
        first_name: $first_name
        last_name: $last_name
        email: $email
        phone: $phone
    }) {
        token
    }
}