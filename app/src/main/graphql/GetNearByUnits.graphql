query GetNearByUnits($lat: Float! $lng: Float!){
    getNearByUnits(input: {
        gps: {
            lat: $lat
            lng: $lng
        }
    }) {
        id
        name
        price
        distance
        type
        updatedAt
    }
}