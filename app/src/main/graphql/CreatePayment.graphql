mutation CreatePayment($email: String! $amount: String! $phone: String!) {
    createPayment(input: {
        phone: $phone
        email: $email
        amount: $amount
    }) {
        success
    }
}