mutation SignIn(
    $phone: String!
) {
    signIn(input: {
        phone: $phone
    }) {
        user {
            is_landlord
            id
            createdAt
            phone
            first_name
            subscribe_retries
            last_name
            avatar {
                upload
            }
        }
        Token
    }
}